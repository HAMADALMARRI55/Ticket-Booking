Ok so now all the functionality is working properly. Now we need to use Tkinter to create GUI for this work. There is a separate file called gui.py where we have to implement it. Both user and admin functionalities will work there